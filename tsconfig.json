{
  "compilerOptions": {
    // ... other compiler options
    "jsx": "react", // Or "react-jsx" if using React 17+ with the new JSX transform
    "esModuleInterop": true, // Recommended (see explanation below)
    "moduleResolution": "node" // Ensure module resolution is set to node
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}